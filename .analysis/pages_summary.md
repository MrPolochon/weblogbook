### /
- fichier: src/app/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): (non détectés via regex)

### /admin
- fichier: src/app/(app)/admin/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: password_reset_requests, vols, plans_vol, aeroschool_responses, hangar_market_reventes
- Imports composants (local): (non détectés via regex)

### /admin/aeroschool
- fichier: src/app/(app)/admin/aeroschool/page.tsx
- Appels API repérés: /api/aeroschool/forms
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): (non détectés via regex)

### /admin/aeroschool/[id]
- fichier: src/app/(app)/admin/aeroschool/[id]/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): @/components/aeroschool/FormBuilder

### /admin/aeroschool/[id]/responses
- fichier: src/app/(app)/admin/aeroschool/[id]/responses/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): (non détectés via regex)

### /admin/aeroschool/modules
- fichier: src/app/(app)/admin/aeroschool/modules/page.tsx
- Appels API repérés: /api/aeroschool/modules
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): (non détectés via regex)

### /admin/aeroschool/modules/[id]
- fichier: src/app/(app)/admin/aeroschool/modules/[id]/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): (non détectés via regex)

### /admin/alliances
- fichier: src/app/(app)/admin/alliances/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, alliances, compagnies, alliance_membres
- Imports composants (local): (non détectés via regex)

### /admin/armee
- fichier: src/app/(app)/admin/armee/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, felitz_comptes, vols
- Imports composants (local): (non détectés via regex)

### /admin/avions
- fichier: src/app/(app)/admin/avions/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles
- Imports composants (local): (non détectés via regex)

### /admin/compagnies
- fichier: src/app/(app)/admin/compagnies/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: compagnies, profiles
- Imports composants (local): (non détectés via regex)

### /admin/compagnies/[id]/logbook
- fichier: src/app/(app)/admin/compagnies/[id]/logbook/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, compagnies, vols
- Imports composants (local): @/components/VolDeleteButton

### /admin/documents
- fichier: src/app/(app)/admin/documents/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: document_sections
- Imports composants (local): (non détectés via regex)

### /admin/employes
- fichier: src/app/(app)/admin/employes/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, compagnies, compagnie_employes
- Imports composants (local): (non détectés via regex)

### /admin/felitz-bank
- fichier: src/app/(app)/admin/felitz-bank/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, felitz_comptes
- Imports composants (local): (non détectés via regex)

### /admin/hangar-market
- fichier: src/app/(app)/admin/hangar-market/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, hangar_market_config, hangar_market, hangar_market_reventes
- Imports composants (local): (non détectés via regex)

### /admin/inventaire
- fichier: src/app/(app)/admin/inventaire/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles
- Imports composants (local): (non détectés via regex)

### /admin/ips
- fichier: src/app/(app)/admin/ips/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): (non détectés via regex)

### /admin/licences
- fichier: src/app/(app)/admin/licences/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, types_avion
- Imports composants (local): (non détectés via regex)

### /admin/militaire
- fichier: src/app/(app)/admin/militaire/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, vols
- Imports composants (local): @/components/VolDeleteButton

### /admin/militaire/vol/[id]
- fichier: src/app/(app)/admin/militaire/vol/[id]/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, vols
- Imports composants (local): @/components/VolDeleteButton

### /admin/password-reset-requests
- fichier: src/app/(app)/admin/password-reset-requests/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, password_reset_requests
- Imports composants (local): (non détectés via regex)

### /admin/pilotes
- fichier: src/app/(app)/admin/pilotes/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, user_login_tracking
- Imports composants (local): (non détectés via regex)

### /admin/pilotes/[id]
- fichier: src/app/(app)/admin/pilotes/[id]/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, cartes_identite
- Imports composants (local): (non détectés via regex)

### /admin/pilotes/[id]/logbook
- fichier: src/app/(app)/admin/pilotes/[id]/logbook/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, vols, vols_equipage_militaire
- Imports composants (local): @/components/VolDeleteButton

### /admin/plans-vol
- fichier: src/app/(app)/admin/plans-vol/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, plans_vol
- Imports composants (local): (non détectés via regex)

### /admin/reparation
- fichier: src/app/(app)/admin/reparation/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, entreprises_reparation, reparation_employes, reparation_hangars, felitz_comptes
- Imports composants (local): (non détectés via regex)

### /admin/securite
- fichier: src/app/(app)/admin/securite/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, site_config
- Imports composants (local): (non détectés via regex)

### /admin/sid-star
- fichier: src/app/(app)/admin/sid-star/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles
- Imports composants (local): (non détectés via regex)

### /admin/taxes
- fichier: src/app/(app)/admin/taxes/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, taxes_aeroport
- Imports composants (local): (non détectés via regex)

### /admin/types-avion
- fichier: src/app/(app)/admin/types-avion/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, types_avion
- Imports composants (local): (non détectés via regex)

### /admin/vols
- fichier: src/app/(app)/admin/vols/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, vols, types_avion, compagnies
- Imports composants (local): (non détectés via regex)

### /aeroschool
- fichier: src/app/aeroschool/page.tsx
- Appels API repérés: /api/aeroschool/forms
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): (non détectés via regex)

### /aeroschool/[id]
- fichier: src/app/aeroschool/[id]/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): @/components/aeroschool/FormRenderer

### /alliance
- fichier: src/app/(app)/alliance/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: compagnies, compagnie_employes
- Imports composants (local): (non détectés via regex)

### /atc
- fichier: src/app/(atc)/atc/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: atc_sessions, plans_vol, afis_sessions, compagnie_avions, types_avion, profiles, compagnies, vhf_position_frequencies
- Imports composants (local): @/components/PlansEnAttenteModal, @/components/AtcEnLigneModal, @/components/FlightStripBoardWrapper, @/components/FlightStrip

### /atc/admin
- fichier: src/app/(atc)/atc/admin/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, atc_grades, atc_sessions
- Imports composants (local): (non détectés via regex)

### /atc/compte
- fichier: src/app/(atc)/atc/compte/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, atc_grades
- Imports composants (local): @/components/LicencesSection

### /atc/creer-plan
- fichier: src/app/(atc)/atc/creer-plan/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: atc_sessions
- Imports composants (local): (non détectés via regex)

### /atc/documents
- fichier: src/app/(atc)/atc/documents/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: document_sections
- Imports composants (local): @/components/DocumentTree

### /atc/felitz-bank
- fichier: src/app/(atc)/atc/felitz-bank/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, felitz_comptes, felitz_transactions
- Imports composants (local): (non détectés via regex)

### /atc/messagerie
- fichier: src/app/(atc)/atc/messagerie/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, messages
- Imports composants (local): (non détectés via regex)

### /atc/notams
- fichier: src/app/(atc)/atc/notams/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: notams, profiles
- Imports composants (local): (non détectés via regex)

### /atc/plan/[id]
- fichier: src/app/(atc)/atc/plan/[id]/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: plans_vol, profiles, compagnies, compagnie_avions, types_avion, atc_sessions
- Imports composants (local): @/components/TranspondeurBadgeAtc

### /atc/spectateur/[userId]
- fichier: src/app/(atc)/atc/spectateur/[userId]/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, atc_sessions, plans_vol, compagnies, compagnie_avions
- Imports composants (local): (non détectés via regex)

### /compte
- fichier: src/app/(app)/compte/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, cartes_identite
- Imports composants (local): @/components/LicencesSection

### /documents
- fichier: src/app/(app)/documents/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: document_sections
- Imports composants (local): @/components/DocumentTree

### /download
- fichier: src/app/download/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): (non détectés via regex)

### /felitz-bank
- fichier: src/app/(app)/felitz-bank/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: felitz_comptes, profiles, compagnies, felitz_transactions
- Imports composants (local): (non détectés via regex)

### /hangar-market
- fichier: src/app/(app)/hangar-market/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, felitz_comptes, compagnies, inventaire_avions, plans_vol, hangar_market, compagnie_avions, compagnie_locations, hangar_market_config
- Imports composants (local): (non détectés via regex)

### /ifsa
- fichier: src/app/(app)/ifsa/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, ifsa_signalements, ifsa_enquetes, ifsa_sanctions, compagnies, compagnie_employes, autorisations_exploitation
- Imports composants (local): (non détectés via regex)

### /ifsa/licences
- fichier: src/app/(app)/ifsa/licences/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, types_avion
- Imports composants (local): (non détectés via regex)

### /inventaire
- fichier: src/app/(app)/inventaire/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: inventaire_avions, plans_vol
- Imports composants (local): (non détectés via regex)

### /logbook
- fichier: src/app/(app)/logbook/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, vols, plans_vol
- Imports composants (local): @/components/VolDeleteButton

### /logbook/a-confirmer
- fichier: src/app/(app)/logbook/a-confirmer/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: vols
- Imports composants (local): (non détectés via regex)

### /logbook/depot-plan-vol
- fichier: src/app/(app)/logbook/depot-plan-vol/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, plans_vol, compagnie_employes, compagnies, inventaire_avions, compagnie_avions, compagnie_locations
- Imports composants (local): (non détectés via regex)

### /logbook/nouveau
- fichier: src/app/(app)/logbook/nouveau/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, types_avion, compagnies, plans_vol
- Imports composants (local): (non détectés via regex)

### /logbook/plans-vol
- fichier: src/app/(app)/logbook/plans-vol/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, plans_vol
- Imports composants (local): (non détectés via regex)

### /logbook/plans-vol/[id]/modifier
- fichier: src/app/(app)/logbook/plans-vol/[id]/modifier/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, plans_vol
- Imports composants (local): (non détectés via regex)

### /logbook/vol/[id]
- fichier: src/app/(app)/logbook/vol/[id]/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, vols, types_avion, compagnies
- Imports composants (local): (non détectés via regex)

### /login
- fichier: src/app/login/page.tsx
- Appels API repérés: /api/has-admin, /api/site-config, /api/auth/send-login-code, /api/auth/register-login, /api/auth/verify-login-code, /api/auth/forgot-password ...
- Tables Supabase repérées: profiles
- Imports composants (local): (non détectés via regex)

### /ma-compagnie
- fichier: src/app/(app)/ma-compagnie/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: compagnie_employes, compagnies, vols, plans_vol, felitz_comptes
- Imports composants (local): (non détectés via regex)

### /marche-cargo
- fichier: src/app/(app)/marche-cargo/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: aeroport_cargo
- Imports composants (local): (non détectés via regex)

### /marche-passagers
- fichier: src/app/(app)/marche-passagers/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: aeroport_passagers
- Imports composants (local): (non détectés via regex)

### /marketplace
- fichier: src/app/(app)/marketplace/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, felitz_comptes, compagnies, types_avion
- Imports composants (local): (non détectés via regex)

### /messagerie
- fichier: src/app/(app)/messagerie/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: messages, profiles
- Imports composants (local): (non détectés via regex)

### /militaire
- fichier: src/app/(app)/militaire/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, vols, vols_equipage_militaire
- Imports composants (local): @/components/VolDeleteButton

### /militaire/nouveau
- fichier: src/app/(app)/militaire/nouveau/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, armee_avions
- Imports composants (local): (non détectés via regex)

### /notams
- fichier: src/app/(app)/notams/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: notams, profiles
- Imports composants (local): (non détectés via regex)

### /perf-ptfs
- fichier: src/app/(app)/perf-ptfs/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): (non détectés via regex)

### /reparation
- fichier: src/app/(app)/reparation/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): (non détectés via regex)

### /reparation/jeu/[demandeId]
- fichier: src/app/(app)/reparation/jeu/[demandeId]/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): (non détectés via regex)

### /setup
- fichier: src/app/setup/page.tsx
- Appels API repérés: /api/has-admin, /api/setup
- Tables Supabase repérées: (aucune occurrence de .from(...))
- Imports composants (local): (non détectés via regex)

### /siavi
- fichier: src/app/(siavi)/siavi/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: afis_sessions, atc_sessions, profiles, plans_vol
- Imports composants (local): (non détectés via regex)

### /siavi/admin
- fichier: src/app/(siavi)/siavi/admin/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, siavi_grades, afis_sessions
- Imports composants (local): (non détectés via regex)

### /siavi/compte
- fichier: src/app/(siavi)/siavi/compte/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, siavi_grades
- Imports composants (local): @/components/LicencesSection

### /siavi/documents
- fichier: src/app/(siavi)/siavi/documents/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: document_sections
- Imports composants (local): @/components/DocumentTree

### /siavi/felitz-bank
- fichier: src/app/(siavi)/siavi/felitz-bank/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, felitz_comptes, felitz_transactions
- Imports composants (local): (non détectés via regex)

### /siavi/messagerie
- fichier: src/app/(siavi)/siavi/messagerie/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, messages
- Imports composants (local): (non détectés via regex)

### /siavi/plan/[id]
- fichier: src/app/(siavi)/siavi/plan/[id]/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: profiles, afis_sessions, plans_vol, compagnies, compagnie_avions, types_avion
- Imports composants (local): @/components/TranspondeurBadgeAtc

### /signalement
- fichier: src/app/(app)/signalement/page.tsx
- Appels API repérés: (aucun via fetch(/api/))
- Tables Supabase repérées: ifsa_signalements, profiles, compagnies
- Imports composants (local): (non détectés via regex)

